#define _CRT_SECURE_NO_WARNINGS

#include <iostream>
#include <chrono>

using namespace std;



int main()
{
	int a = 10;
	int* ptr_a = &a;
	int** ptr2 = &ptr_a;
	int*** ptr3 = &ptr2;

	cout << a << endl;
	cout << *ptr_a << endl;
	cout << **ptr2 << endl;
	cout << ***ptr3 << endl;

	int sizeY = 10;
	int sizeX = 10;

	int k = sizeX;

	int** mass2D = new int* [sizeY];

	for (int i = 0; i < sizeY; ++i, --k)
	{
		mass2D[i] = new int[k];
	}

	k = sizeX;

	for (int i = 0; i < sizeY; ++i, --k)
	{
		for (int j = 0; j < k; ++j)
		{
			mass2D[i][j] = 0;
			cout << mass2D[i][j] << " ";
		}
		cout << endl;
	}

	for (int i = 0; i < sizeY; ++i)
	{
		delete[] mass2D[i];
	}

	delete[] mass2D;
}
