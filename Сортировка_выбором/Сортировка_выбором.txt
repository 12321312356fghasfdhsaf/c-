 for (int i = 0; i < size; i++)
 {
     digitals4[i] = 1 + rand() % 50; // заполняем массив элементами от 0 до 50
 }

 int min = 0; // для записи минимального значения
 int buf = 0; // для обмена значениями 
 


 for (int i = 0; i < size; i++)
{
	min = i; // запомним номер текущей ячейки, как ячейки с минимальным значением
	// в цикле найдем реальный номер ячейки с минимальным значением
	for (int j = i + 1; j < size; j++)
min = (digitals4[j] < digitals4[min]) ? j : min;//если j=min
	// cделаем перестановку этого элемента, поменяв его местами с текущим
	if (i != min)
  {
      counter2++;//счетчик перестановки "выбором"
buf = digitals4[i];
digitals4[i] = digitals4[min];
digitals4[min] = buf;
	}

 }
    

 //for (int i = 0; i < size; i++) //Вывод отсортированного массива
 //{
     //cout << digitals4[i] << " ";
// }